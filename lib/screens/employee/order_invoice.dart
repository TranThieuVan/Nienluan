import 'package:flutter/material.dart';

class OrderInvoiceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Hóa đơn")),
      body: Center(child: Text("Hiển thị order / in hóa đơn")),
    );
  }
}
